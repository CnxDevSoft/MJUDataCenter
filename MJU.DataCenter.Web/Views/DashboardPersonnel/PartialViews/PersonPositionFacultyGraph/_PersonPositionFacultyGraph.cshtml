@{
    var userAccesToken = User.FindFirst("AccessToken").Value;
}
<div class="col-lg-6">
    <div class="card card-green-theme">
        <div class="card-header border-0">
            <div class="d-flex justify-content-between">
                <h3 class="card-title">กราฟแสดงจำนวนบุคลากรแยกตามตำแหน่งทางวิชาการ และสังกัด</h3>
            </div>
        </div>
        <div class="card-body bg-light-sea-green">
            <div class="position-relative mb-4 text-center">
                <div class="btn-group btn-group-toggle btn-toggle-green" data-toggle="buttons">
                    <label class="btn btn-success label-default-bg active" id="switch-personPositionFaculty-label-canvas">
                        <input type="radio" name="options" id="switch-personPositionFaculty-chart-1" autocomplete="off" checked="">
                        <i class="fa fa-chart-bar" aria-hidden="true"></i><br /> มุมมองกราฟ
                    </label>
                    <label class="btn btn-success label-default-bg btn-toggle-green" id="switch-personPositionFaculty-label-table">
                        <input type="radio" name="options" id="switch-personPositionFaculty-chart-2" autocomplete="off">
                        <i class="fa fa-table" aria-hidden="true"></i><br /> มุมมองตาราง
                    </label>
                </div>
                <a class="btn btn-app" style="position: absolute;right: 0;" id="personPositionFaculty-datasource">
                    <i class="fas fa-info"></i> ข้อมูล
                </a>
            </div>
            <br />
            <br />
            <br />
            <div class="position-relative mb-4" id="personPositionFaculty-chart-canvas">
                <canvas id="personPositionFaculty-chart" style="min-height: 300px; height: 300px; max-height: 300px; max-width: 100%;"></canvas>
            </div>
            <div class="position-relative mb-4" id="personPositionFaculty-chart-table" style="display:none;">
                <partial name="PartialViews/PersonPositionFacultyGraph/_personPositionFacultyGraphDataTable" />
            </div>
        </div>
    </div>
</div>

<script>
    $(function () {
        PersonPositionFacultyGraph('@userAccesToken','@User.Identity.Name');
        ToggleChart("personPositionFaculty");
        $('#personPositionFaculty-datasource').click(function () {
            PersonPositionFacultyGraphDS('@userAccesToken','@User.Identity.Name')
         });
    })
</script>
<partial name="PartialViews/PersonPositionFacultyGraph/_personPositionFacultyGraphDataSource" />